allprojects {
    task trace {
        doLast { task ->
            println "trace: $task.project.name"
        }
    }
}
configure([project(':consumer'), project(':dispatcher')]) {
    apply plugin: 'java'
}
configure([project(':consumer'), project(':query-server')]) {
    apply plugin: 'war'
}
