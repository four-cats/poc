allprojects {
    task trace {
        doLast { task ->
            println "trace: $task.project.name"
        }
    }
}
configure([project(':consumer'), project(':dispatcher'), project(':query-server')]) {
    apply plugin: 'java'
}
